# 引き継ぎメモ（次セッション用）

最終更新: 2026-09-16

> ## 🔴 次のセッションで最初に読むファイル
>
> **デザイン改修は2026-09-16に第1〜第7段階まで完了し、すべてライブ反映・確認済み。**
> 何をどう直したか（実測値つき）と、**次にやること**は `DESIGN-TODO.md` にまとめてある。
> **このファイル（HANDOFF.md）の「1. この作業のやり方」を読んでから、`DESIGN-TODO.md` へ進むこと。**
>
> **2026-09-16 時点のフェーズ: 公開準備。** ユーザーが「一旦これで公開準備をする。
> あとは自分でGitHubから文章を修正してみる」と述べた。以下の2点を**公開前に必ず確認**:
>
> 1. **`noindex` / `robots.txt` は外さない。** これは技術的な手順ではなく、
>    **病院としての正式許可に紐づくスイッチ**（実名・住所を載せているため。
>    詳細は `README.md`「⚠️ 実名・住所の暫定公開について」）。許可が下りるまで残す
> 2. ~~「修練医の声」と「他己紹介」の本文がサンプル文~~ → **2026-09-16 に解消。**
>    ユーザーが6件すべてを実際の文面に書き換えた（`8133f58` ほか）
>
> 回答待ちの項目は「4. 未回答の質問」を参照。**催促はしない。**

> このファイルは**作業用の内部メモ**。運営管理会社へ渡す必要はない（渡すのは
> `index.html` `recruit.html` `style.css` `script.js` `robots.txt` `media/` の6点）。
> `DESIGN-TODO.md` も同じく内部メモ。
> プロジェクトの恒久的な仕様・判断根拠は `README.md` が正。こちらは「いまどこまで
> 進んでいて、次に何をするか」だけを書く。

> ⚠️ **ユーザーはGitHubの画面上で `recruit.html` を直接編集する。** 2026-09-15 のセッション中だけで
> 12コミット入った（`c905874`〜`a24ed23`、および `d7c29df` `e5000f0`）。**作業開始前に必ず `git pull`**。

---

## 1. この作業のやり方（必ず守る）

### ① ローカル編集だけでは終わっていない

**編集 → commit → push → ライブURLで確認、までが1セット。**
過去に「ホームページが修正されていません」と2回指摘されている。原因はいずれも
ローカルを直しただけで push していなかったこと。ファイルを直した時点で
「できました」と報告しない。

反映確認は curl でライブを叩いて実際の文字列を見る。GitHub Pages への反映は
**おおむね20〜45秒**（＝ポーリング3回目くらいで通ることが多い）。

```bash
curl -s "https://shingoochiai-1118.github.io/icu-recruit-site/recruit.html?cb=1" | grep -c "確認したい文字列"
```

ブラウザは古いHTMLをキャッシュするので、確認時は `?cb=1` のようなダミークエリを付ける。

### ② CSS / JS を変えたら `?v=` を上げる

`index.html` と `recruit.html` の `<head>`（両方とも同じ値に揃える）。
**現在 `style.css?v=20260916g` / `script.js?v=20260915b`（両ページとも同じ値）。**
同日に複数回変えるときは末尾に英字を足す（`e` → `f`）。
HTMLの文章だけ直した場合は触らなくてよい。

### ③ 実名・顔写真は勝手に push しない

`README.md` の「実名・住所の暫定公開について」参照。本人同意の確認が取れるまで
コミットを保留する。イラスト（実在人物の似顔絵ではないもの）はこの制約の対象外。

### ④ 検証ツールの使い分け（ハマりどころ）

| やりたいこと | 使うもの |
|---|---|
| 画面の見た目を確認 | **ヘッドレスChrome** で全画面PNG → PIL で切り出して読む |
| 要素の寸法・DOMの状態 | Browser pane の `javascript_tool` |
| レスポンシブ確認 | Browser pane の `resize_window`（実機エミュレーション） |

- Browser pane の**スクリーンショットはスクロールすると古いフレームを返す**ことがある。見た目の確認には使わない。
- ヘッドレスChromeの**狭い幅（375〜390px）のキャプチャは信用しない**。もっと広い幅で
  レイアウトが描画されるため、実際には収まっているものがはみ出して見える。**2026-09-16 にも
  1回引っかかった**（ヒーローの数字カードがはみ出して見えたが、実測は 16..181 / 195..359 で収まっていた）。
  レスポンシブの判定は `resize_window` + `javascript_tool` の実測を信じる。スマホの見た目を
  どうしても見たいときは Browser pane のスクリーンショット（ページ先頭のまま、スクロールせずに撮る）。
- ブロック要素の文字幅は `getBoundingClientRect()` では測れない（箱の幅が返る）。`document.createRange().selectNodeContents(el).getBoundingClientRect().width` を使う。

ヘッドレスChromeで縦長ページを撮ったあと、目的のセクションを切り出す手順:

```bash
# 1) 全画面を撮る
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --hide-scrollbars --virtual-time-budget=5000 --window-size=1280,20000 \
  --screenshot=full.png "http://localhost:8743/recruit.html"
# 2) offsetTop は Browser pane を 1280px 幅にして javascript_tool で取る（幅が違うと位置がずれる）
# 3) PIL で crop して Read する
```

ローカルプレビューは `.claude/launch.json` の `static-site`（`preview_start` で起動、ポート8743）。

---

## 2-A. 2026-09-16（デザイン改修セッション）で完了したこと

デザイナー2名の評価にもとづく改修を第1〜第5段階まで実施。**5コミット、すべてライブ確認済み。**
詳細・実測値・次にやることは **`DESIGN-TODO.md`** を見ること。ここでは要点だけ。

| コミット | 内容 |
|---|---|
| `c5ac27e` | 未完成に見える箇所を読者から隠す（PLACEHOLDERバッジ6件・学びのまとめ3枚・チームの雰囲気・氏名未記入カード） |
| `a6c1e54` | ヒーローの白文字を読めるようにする（最悪部 1.41:1 → 4.69:1） |
| `36f256d` | コントラストの小さな不合格箇所と見出し階層（h4/h5 → h3） |
| `f5b7f2a` | 文字サイズ・英語併記の斜体解除・目次の固定 |
| `28f78ae` | ダミー枠の英語表記、ヘッダーの押せないリンク、残りのコントラスト |
| `487032e` | 病院外観の写真をヒーローから独立した帯に移す（ユーザー指示） |
| `044b66b` | 写真がまだない箇所の見せ方（index=枠を出さない / recruit=アイコンのパネル） |

### このセッションで作った「消してはいけない」仕掛け

1. **`style.css` の先頭の `[hidden] { display: none !important; }`**。
   `display: flex / grid` を指定した要素は、それだけでブラウザ標準の `[hidden]` を打ち消す。
   原稿や写真が揃うまで隠している箇所（チームの雰囲気・氏名未記入カード等）はこの1行に依存している
2. **「学びのまとめ」3枚はコメントアウトで隠してある**（`hidden` ではない）。親グリッドの
   `data-collapse-after="4"` のトグルで `[要記入]` のカードが開いてしまうため
3. **ヘッダーのクロスリンクは `sticky` で右端に貼り付けてある**。以前は 1280px で
   201pxあふれて完全に画面外に出ており、押せなかった
4. **ヒーローの黒の膜（0.38）と、装飾の「黒」4%の円**。写真を帯に移したあとも
   膜は必要（無地のグラデーションだけでは白文字が index 2.41:1）。装飾の円を白に
   戻すと数字カードのコントラストが下がる。**装飾に白を足すときは必ず画素を測り直す**
5. **写真帯のキャプションの膜は下44pxを 0.62 で一定**にしている。単純な
   グラデーションにすると文字の位置で薄くなり、雲の白が透けて 2.26:1 になる

### ユーザーが判断した項目（2026-09-16）

| 項目 | 回答 |
|---|---|
| 隠す範囲 | チームの雰囲気／学びのまとめ3枚／他己紹介「氏名（仮）」カード **→ 隠す**。**index設備の空枠4つは隠さない** |
| 看護師の補足文への一文追加 | **この文で追加してよい** → 反映済み |
| CKRT / CHDF-3 の表記 | **CKRT（CHDF）と併記** → 反映済み |
| index の最初の1画面の数字カード | 「どこのことかよくわからない」→ 注釈付きの画面写真で説明済み。**回答待ち** |

---

## 2-0. 2026-09-15（写真セッション）で完了したこと

**許可済みの写真10枚を掲載・push・ライブ確認まで完了**（`eb5fe19`）。

- 顔写真7名（index #staff）: 井澤／酒井／依光／落合／江田／森／小波本
- 設備3点: 人工呼吸器・CKRT装置・医師控室（index #equipment）、CKRT（recruit 機材ギャラリー）
- `media/src/prepare-photo.sh` を追加（`d7c7873`）。HEIC変換・縮小・**Exif/GPS完全削除**を1コマンドで行う
- 元ファイルは `~/icu-photos-inbox`（リポジトリ外・非コミット）

### 写真作業で必ず守ること

1. **1枚ずつ原寸で患者情報の写り込みを確認する。** 医師控室の写真は壁の集中モニターに
   患者名・病床番号が原寸で読めたため、ぼかしを入れてから掲載した
2. **Exif は必ず落とす**（`prepare-photo.sh` を通す）。院内写真にはGPSが埋まっている
3. **HTMLの一括置換は危険。** このセッションで2回壊した。原因は
   ①`.*?` が複数カードをまたいで飲み込んだ ②閉じ `</div>` を1つ多く数えて氏名まで消した。
   置換後は必ず `staff-card` と `staff-name` の**件数が変わっていないこと**を検証する

### 追加対応（同日・ユーザー指示）

- 外間師長を **外間 ひとみ**として師長枠に、喜名副師長をその隣（副師長2枠のうち1つめ）に配置
- 落合・小波本の顔写真を**顔が中央に来るよう切り出し直した**（横方向のずれ＋縦は寄って調整）
- recruit の他己紹介（#peer-intro）に酒井・小波本の写真を追加。`.peer-avatar.has-photo` で
  イラスト用の内側余白を消して円いっぱいに表示する

### 保留中

| 項目 | 理由 |
|---|---|
| **喜名副師長の名** | **姓のみ表示中**。他はフルネームなので、判明し次第 `.staff-name` を差し替える |

### さらに追加対応（同日・ユーザー指示 その2）

- 小波本の顔写真を再調整（前回は右へ寄せすぎて左にずれていた）。**他己紹介と同じ画像を
  共用しているので、1枚直せば両方直る**
- 師長の氏名を **外間 美和子**に修正（前回「外間 ひとみ」で登録したのは誤り）
- **病院外観写真を両ページのヒーロー背景に採用**。グラデーションを不透明度88〜93%で
  重ねて建物をうっすら透かす。色はページごとに違うため `--hero-grad-soft` という変数を
  `:root` と `:root[data-theme="recruit"]` の両方に定義し、`#hero` は1か所だけにした
  （最初は index 限定で入れたが、ユーザー指示で採用ページにも展開）
  - 白文字とのコントラストは 2.95:1 → 2.84:1 でほぼ変化なし。**元の緑グラデーション自体が
    WCAG AA（4.5:1）未達**で、これは写真とは無関係の以前からの課題

### さらに追加対応（同日・ユーザー指示 その3）

報告済みで未対応だった指摘のうち、判断が要らないものをまとめて処理した。

- 人工呼吸器の写真の「点検済み」タグに写っていた**点検者の手書き氏名にぼかしを入れた**
  （同意を取っていない職員の氏名のため。掲載サイズでは元々読めなかったが二重の安全策）。
  ファイルは差し替え済みで、Exif が付いていないことも再確認した
- recruit 機材ギャラリーの**脳波計の枠の英語表記を `Electroencephalography` に修正**
  （`Mechanical Ventilator` のままだった）。他の `class="sub"` も全件確認し、残りのずれはなし
- `script.js` の `?v=` を index / recruit とも **`20260915b`** に統一（index が `20260825` のままだった）

### 写真について報告済み・未対応の点

- ~~CKRT実機のラベルは `CHDF-3`（サイト表記は CKRT）。呼称が違う~~
  → **2026-09-16 に解決。ユーザーの選択で「CKRT（CHDF）」と併記した**（両ページ反映済み）

---

## 2. それ以前のセッションで完了したこと

| コミット | 内容 |
|---|---|
| `2119d2d` | 修練医の声のアバターをイラスト3枚に差し替え |
| `06e26ef` | リンク先のない「面会について」「外来受付」を削除 |
| `0288b01` | recruitのCTAを1本に統合、フッター関連リンクを病院サイトへ接続 |
| `a041ac5` | CTAのリンク先を設定、`<button>` → `<a>` に変換 |
| `65d2bde` | ECMO件数 20〜25 → **15〜20**、スタッフ名「高橋 慶太」→「**高橋 慶多**」 |

**リンク先未設定のボタン・リンクはゼロになった。** 引き渡し前の懸案だったCTA問題は解決済み。

---

## 3. 残っている作業

> **2026-09-16 に実物と照合して書き直した。** 以前ここに書いてあった
> 「`[要記入]` が18箇所」は古い。キャリアパス9件と機材ギャラリー4件は
> **ユーザー本人が記入済み**で、勉強会記事9件は**コメントアウトして隠してある**。
> **読者に見える `[要記入]` は現在0件**（ブラウザのDOMで確認済み）。

### A. 原稿が必要なもの（ユーザーが書くもの）

| 場所 | 件数 | 状態 |
|---|---|---|
| 学びのまとめ（勉強会記事）3枚 | 9 | **コメントアウト中**。テーマ・一言コメント・参考文献。原稿が入ったらコメントを外す |
| `#residents` 修練医の声 3枠 | 3 | 本文は仮のもの。氏名 T.M・S.N・K.K は設定済み。**PLACEHOLDERバッジは撤去済み** |
| `#peer-intro` 他己紹介 3枠 | 3 | うち1枠（氏名未定）は `hidden`。**PLACEHOLDERバッジは撤去済み** |

差し替え予定の場所は、それぞれHTMLコメントで「差し替え予定」と書いてある
（`grep -n "差し替え予定" recruit.html`）。

**進め方の合意事項**: 文面を指定してもらってもよいし、「こういう内容を書きたい」という
箇条書きをもらえれば、こちらで文章に起こして提案する。

### B. その他

| 項目 | 状態 |
|---|---|
| 他己紹介② の氏名 | 未定。**カードごと `hidden` にしてある**。決まったら `hidden` を外して `.peer-name` を差し替え |
| 喜名副師長の名（下の名前） | 姓のみ表示中。判明したら `.staff-name` と `alt` の両方を直す |
| 他己紹介のアバター3枠 | イラスト未挿入。**「修練医の声と同じようにイラストを入れるか」を質問済み・回答待ち** |
| 写真素材 | `img-placeholder` のダミー枠が recruit 12件 / index 10件（うち6件は意図的なスタッフアバター）。実写真の反映は正式許可後 |
| `noindex` / `robots.txt` | 正式公開時に外す（**外し忘れると検索に一切載らない**） |
| 隠している箇所を戻す | チームの雰囲気・学びのまとめ・氏名未記入のカード。戻し方は各HTMLのコメントに書いてある |

### C. デザイン面で残っているもの

**`DESIGN-TODO.md` の「🔴 次にやること」を見ること。** 要点だけ:

- ユーザーの回答待ち3件（index の数字カード／修練医の声の直後の1行導線／index設備セクションの短縮）
- Google Fonts のウェイト数削減（見た目が変わる可能性があるので未着手）

> 2026-09-16 に、以前ここに「未着手」として挙げていた項目のうち3件は完了した:
> `.section-label` のコントラスト（2.91:1 → 6.2:1）／ヘッダーナビの窮屈さ
> （リンクが押せなくなっていたのを sticky で解決）／`fonts.gstatic.com` への `preconnect`。
> 「本文が12〜13pxで小さい」も、1行35〜45字に収まっていない箇所だけ大きくした。

---

## 4. 未回答の質問（次に聞くこと）

**催促はしない。** 回答が来たものから着手する。

1. **index の最初の1画面の数字カード**（`DESIGN-TODO.md` の A）。2026-09-16 に
   注釈付きの画面写真を見せて説明した。回答待ち
2. **修練医の声の直後に1行の導線を足すか**（`DESIGN-TODO.md` の B）。新しい文章を足すので承認が必要
3. **index 設備セクションを「もっと見る」で短縮するか**（`DESIGN-TODO.md` の C）
4. **他己紹介の3枠にもイラストを入れるか。** 入れるなら、修練医の声と同じ手順で処理できる（`media/src/README.md` に切り出し手順あり）
5. **編集手順書（`EDITING.md`）を作るか。** ユーザーから「PCから直接文章を修正する方法はあるか」と質問があり、VS Codeでの編集手順を口頭で説明済み。画面に沿った手順書をリポジトリに置くことを提案したが、回答待ち

---

## 5. ユーザーとの共同作業で注意すること

**ユーザー自身が GitHub / VS Code で直接編集する可能性がある。**
前セッションで編集方法を案内した（VS Code は導入済み、git認証はキーチェーンに保存済み）。

そのため、**作業を始める前に `git pull` して最新を取り込むこと**。ローカルが古いまま
編集すると、ユーザーの修正を上書きしてしまう。

```bash
cd /Users/shingoochiai/icu-recruit-site && git pull --ff-only
```

ユーザーは医師でエンジニアではない。HTML/CSSの用語をそのまま使わず、
「何が起きるか」「どこを触ればよいか」を具体的に書く。

---

## 6. 素材まわり

- `media/resident-avatar-{1,2,3}.png`（各256×256・背景透過）が「修練医の声」のアバター
- 元絵と切り出しパラメータは `media/src/`。**サイトからは参照されない**
- 切り出しは**パネルを1枚ずつ分離してから**正方形を取る。元画像のまま切ると隣の人物が写り込む
- 背景を透過のままにしているので、`.resident-avatar` の `--primary-xlight` が透けて円形の地色になる

---

## 7. ユーザーによる直接編集（2026-09-15、重要）

このメモを書いている最中に push が `non-fast-forward` で弾かれた。原因は、**ユーザーが
GitHubのウェブ画面で `recruit.html` を10コミット分編集していた**こと（`c905874`〜`a24ed23`、
コミットメッセージはすべて `Update recruit.html`）。前セッションで編集方法を案内した直後で、
さっそく実践されている。

→ **`git pull --ff-only`（または `--rebase`）を毎回必ず先に実行すること。** これは想定上の
リスクではなく、すでに1回発生した実績のある事象。

### ユーザーが入れた内容

- **症例数の下方修正**: 年間新規入室患者数 1,000〜1,200 → **700〜800**（ヒーローは 1,000+ → **700+**）、ECMO 15〜20 → **10〜15**
- **`#schedule` の「裏側を見る」4枚すべてを記入**（回診前の60分／レジデント勉強会 or レクチャー／引き継ぎで伝えること／連絡が入ってから入室までの動き）
- **`#education` 機材ギャラリー4枠すべてを記入**。あわせて機材の入れ替えを実施:
  - CHDF → **CKRT**
  - 人工呼吸器 → **脳波計**
  - 見出しも「最新設備での実践研修」→「多様なデバイスを用いた実践研修」に変更

### 入れ替えの際の取りこぼし（解決済み）

日本語ラベルだけ差し替えて英語のサブラベルが古いままになっていた件は、すべて解決した。

- 脳波計の `Mechanical Ventilator` → `Electroencephalography` に修正（`b5aaa4b`）
- CKRT の枠は実写真に差し替わったため、古いサブラベルごとなくなった
- **2026-09-16 以降、ダミー枠の英語サブラベル（`.sub`）はCSSで非表示にしている**ので、
  この種の取りこぼしは読者には見えない（`.img-placeholder span.sub { display: none }`）

- 本文中に **「ベットサイド」が2箇所**（標準表記は「ベッドサイド」）。ただし本人の文章なので、指摘はしても無断で直さない。
- `equip-block-lead` は「PiCCO・スワンガンツ・CKRT・脳波計」に更新済みで、こちらは整合している。

### 記入済みテキストの扱い

ユーザーが書いた文章は**本人の原稿**。誤字や英訳の不整合を見つけたら報告はするが、
文意に関わる書き換えは提案にとどめ、指示があってから反映する。
