# 引き継ぎメモ（次セッション用）

最終更新: 2026-09-15 / **このメモ作成中に、ユーザー本人がGitHub上で recruit.html を10コミット編集していた**（`c905874`〜`a24ed23`）。下記「7. ユーザーによる直接編集」を最初に読むこと。

> このファイルは**作業用の内部メモ**。運営管理会社へ渡す必要はない（渡すのは
> `index.html` `recruit.html` `style.css` `script.js` `robots.txt` `media/` の6点）。
> プロジェクトの恒久的な仕様・判断根拠は `README.md` が正。こちらは「いまどこまで
> 進んでいて、次に何をするか」だけを書く。

---

## 1. この作業のやり方（必ず守る）

### ① ローカル編集だけでは終わっていない

**編集 → commit → push → ライブURLで確認、までが1セット。**
過去に「ホームページが修正されていません」と2回指摘されている。原因はいずれも
ローカルを直しただけで push していなかったこと。ファイルを直した時点で
「できました」と報告しない。

反映確認は curl でライブを叩いて実際の文字列を見る。GitHub Pages への反映は
**おおむね20〜45秒**（＝ポーリング3回目くらいで通ることが多い）。

```bash
curl -s "https://shingoochiai-1118.github.io/icu-recruit-site/recruit.html?cb=1" | grep -c "確認したい文字列"
```

ブラウザは古いHTMLをキャッシュするので、確認時は `?cb=1` のようなダミークエリを付ける。

### ② CSS / JS を変えたら `?v=` を上げる

`index.html` と `recruit.html` の `<head>`（両方とも同じ値に揃える）。
**現在 `style.css?v=20260915e` / `script.js?v=20260915b`。**
同日に複数回変えるときは末尾に英字を足す（`e` → `f`）。
HTMLの文章だけ直した場合は触らなくてよい。

### ③ 実名・顔写真は勝手に push しない

`README.md` の「実名・住所の暫定公開について」参照。本人同意の確認が取れるまで
コミットを保留する。イラスト（実在人物の似顔絵ではないもの）はこの制約の対象外。

### ④ 検証ツールの使い分け（ハマりどころ）

| やりたいこと | 使うもの |
|---|---|
| 画面の見た目を確認 | **ヘッドレスChrome** で全画面PNG → PIL で切り出して読む |
| 要素の寸法・DOMの状態 | Browser pane の `javascript_tool` |
| レスポンシブ確認 | Browser pane の `resize_window`（実機エミュレーション） |

- Browser pane の**スクリーンショットはスクロールすると古いフレームを返す**ことがある。見た目の確認には使わない。
- ヘッドレスChromeの**狭い幅（390px等）のキャプチャは横スクロールを誤検出する**。レスポンシブの判定は `resize_window` + `javascript_tool` の実測を信じる。
- ブロック要素の文字幅は `getBoundingClientRect()` では測れない（箱の幅が返る）。`document.createRange().selectNodeContents(el).getBoundingClientRect().width` を使う。

ヘッドレスChromeで縦長ページを撮ったあと、目的のセクションを切り出す手順:

```bash
# 1) 全画面を撮る
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --hide-scrollbars --virtual-time-budget=5000 --window-size=1280,20000 \
  --screenshot=full.png "http://localhost:8743/recruit.html"
# 2) offsetTop は Browser pane を 1280px 幅にして javascript_tool で取る（幅が違うと位置がずれる）
# 3) PIL で crop して Read する
```

ローカルプレビューは `.claude/launch.json` の `static-site`（`preview_start` で起動、ポート8743）。

---

## 2. 直近セッションで完了したこと

| コミット | 内容 |
|---|---|
| `2119d2d` | 修練医の声のアバターをイラスト3枚に差し替え |
| `06e26ef` | リンク先のない「面会について」「外来受付」を削除 |
| `0288b01` | recruitのCTAを1本に統合、フッター関連リンクを病院サイトへ接続 |
| `a041ac5` | CTAのリンク先を設定、`<button>` → `<a>` に変換 |
| `65d2bde` | ECMO件数 20〜25 → **15〜20**、スタッフ名「高橋 慶太」→「**高橋 慶多**」 |

**リンク先未設定のボタン・リンクはゼロになった。** 引き渡し前の懸案だったCTA問題は解決済み。

---

## 3. 残っている作業

### A. `[要記入]` — 実際の穴は 18 箇所

`grep -c "\[要記入\]" recruit.html` は **20** を返すが、うち2件は開発者向けの
HTMLコメント内（416行目・466行目）なので**画面に出る穴は18**。
**スケジュールの裏側8件と機材ギャラリー4件は、ユーザー本人が記入済み**（下記7参照）。

| セクション | 件数 | 中身 |
|---|---|---|
| `#education` キャリアパス | 9 | 1年目／2年目／3年目以降 ×（担当する役割・習得する手技・節目） |
| `#dispatches` 勉強会記事 | 9 | 3記事 ×（テーマ・一言コメント・参考文献）。**分量最大** |

**進め方の合意事項**: 文面を指定してもらってもよいし、「こういう内容を書きたい」という
箇条書きをもらえれば、こちらで文章に起こして提案する（後者を提案済み、まだ未着手）。

### B. コメント本文 — `PLACEHOLDER` バッジ 6件

`grep -n "placeholder-tag" recruit.html` は7件ヒットするが、740行目の1件は
勉強会記事の「準備中 — 近日公開予定」なので別物。差し替え待ちは6件:

- `#residents` 修練医の声 3枠（548 / 562 / 576行目）— 氏名 T.M・S.N・K.K は設定済み
- `#peer-intro` 他己紹介 3枠（605 / 620 / 636行目）

### C. その他

| 項目 | 状態 |
|---|---|
| 他己紹介② の氏名 | **未定**（`氏名（仮）` のまま1件）。決まり次第 `.peer-name` を差し替え |
| 他己紹介のアバター3枠 | イラスト未挿入。**「修練医の声と同じようにイラストを入れるか」を質問済み・回答待ち** |
| 写真素材 | `img-placeholder` のダミー枠が recruit 13件 / index 22件。実写真の反映は正式許可後 |
| `noindex` / `robots.txt` | 正式公開時に外す（外し忘れると検索に一切載らない） |

### D. 初回レビューで挙げたが未着手の項目

ユーザーがまだ判断していないもの。催促はしない。

- 本文が12〜13pxで小さい
- `.section-label` のコントラスト比 2.91:1（WCAG AA の 4.5:1 未達）
- ヘッダーナビが約1400px以下で窮屈
- Google Fonts のウェイト数削減 ＋ `fonts.gstatic.com` への `preconnect` 追加

---

## 4. 未回答の質問（次に聞くこと）

1. **他己紹介の3枠にもイラストを入れるか。** 入れるなら、修練医の声と同じ手順で処理できる（`media/src/README.md` に切り出し手順あり）
2. **編集手順書（`EDITING.md`）を作るか。** ユーザーから「PCから直接文章を修正する方法はあるか」と質問があり、VS Codeでの編集手順を口頭で説明済み。画面に沿った手順書をリポジトリに置くことを提案したが、回答待ち

---

## 5. ユーザーとの共同作業で注意すること

**ユーザー自身が GitHub / VS Code で直接編集する可能性がある。**
前セッションで編集方法を案内した（VS Code は導入済み、git認証はキーチェーンに保存済み）。

そのため、**作業を始める前に `git pull` して最新を取り込むこと**。ローカルが古いまま
編集すると、ユーザーの修正を上書きしてしまう。

```bash
cd /Users/shingoochiai/icu-recruit-site && git pull --ff-only
```

ユーザーは医師でエンジニアではない。HTML/CSSの用語をそのまま使わず、
「何が起きるか」「どこを触ればよいか」を具体的に書く。

---

## 6. 素材まわり

- `media/resident-avatar-{1,2,3}.png`（各256×256・背景透過）が「修練医の声」のアバター
- 元絵と切り出しパラメータは `media/src/`。**サイトからは参照されない**
- 切り出しは**パネルを1枚ずつ分離してから**正方形を取る。元画像のまま切ると隣の人物が写り込む
- 背景を透過のままにしているので、`.resident-avatar` の `--primary-xlight` が透けて円形の地色になる

---

## 7. ユーザーによる直接編集（2026-09-15、重要）

このメモを書いている最中に push が `non-fast-forward` で弾かれた。原因は、**ユーザーが
GitHubのウェブ画面で `recruit.html` を10コミット分編集していた**こと（`c905874`〜`a24ed23`、
コミットメッセージはすべて `Update recruit.html`）。前セッションで編集方法を案内した直後で、
さっそく実践されている。

→ **`git pull --ff-only`（または `--rebase`）を毎回必ず先に実行すること。** これは想定上の
リスクではなく、すでに1回発生した実績のある事象。

### ユーザーが入れた内容

- **症例数の下方修正**: 年間新規入室患者数 1,000〜1,200 → **700〜800**（ヒーローは 1,000+ → **700+**）、ECMO 15〜20 → **10〜15**
- **`#schedule` の「裏側を見る」4枚すべてを記入**（回診前の60分／レジデント勉強会 or レクチャー／引き継ぎで伝えること／連絡が入ってから入室までの動き）
- **`#education` 機材ギャラリー4枠すべてを記入**。あわせて機材の入れ替えを実施:
  - CHDF → **CKRT**
  - 人工呼吸器 → **脳波計**
  - 見出しも「最新設備での実践研修」→「多様なデバイスを用いた実践研修」に変更

### ⚠️ 入れ替えの際の取りこぼし（未修正・ユーザーに報告済み）

日本語ラベルだけ差し替えて、**英語のサブラベルが古いまま**になっている。現在ライブでも
この状態。**勝手に直さず、ユーザーの指示を待つこと**（本人の原稿なので）。

| 行 | 日本語ラベル | 英語サブラベル（古いまま） | 本来 |
|---|---|---|---|
| 444 | CKRT | `Continuous Hemodiafiltration · photo` | Continuous Kidney Replacement Therapy |
| 451 | 脳波計 | `Mechanical Ventilator · photo` | Electroencephalogram など |

- 本文中に **「ベットサイド」が2箇所**（標準表記は「ベッドサイド」）。ただし本人の文章なので、指摘はしても無断で直さない。
- `equip-block-lead` は「PiCCO・スワンガンツ・CKRT・脳波計」に更新済みで、こちらは整合している。

### 記入済みテキストの扱い

ユーザーが書いた文章は**本人の原稿**。誤字や英訳の不整合を見つけたら報告はするが、
文意に関わる書き換えは提案にとどめ、指示があってから反映する。
