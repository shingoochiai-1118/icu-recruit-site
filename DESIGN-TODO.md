# デザイン改修メモ

作成: 2026-09-16 / 最終更新: 2026-09-16（改修４セッション）
対象: `index.html`（トップ・緑） `recruit.html`（採用・ティール） `style.css`（共通）

プロのウェブデザイナー2名に、2ページを独立に評価してもらった結果をもとにした作業リスト。
**依頼主の狙いは「見やすさ最重視」「最後まで読まれること」の2点。**

> 作業のやり方（pull / push / ライブ確認 / `?v=` の扱い / 検証ツールの使い分け）は
> **`HANDOFF.md` の「1. この作業のやり方」を必ず先に読むこと。** ここでは繰り返さない。

---

## ✅ 2026-09-16 に完了した分（ライブ反映・確認済み）

5コミット。すべて `curl` でライブの実物を確認してある。

| コミット | 内容 |
|---|---|
| `c5ac27e` | 第1段階: 未完成に見える箇所を読者から隠す |
| `a6c1e54` | 第2段階: ヒーローの白文字を読めるようにする |
| `36f256d` | 第3段階: コントラストの小さな不合格箇所と見出し階層 |
| `f5b7f2a` | 第4段階: 文字サイズ・英語併記・目次 |
| `28f78ae` | 第5段階: ダミー枠の英語表記、ヘッダーの押せないリンク、残りのコントラスト |
| `487032e` | 第6段階: 病院外観の写真をヒーローから独立した帯に移す（ユーザー指示） |

### 第1段階（隠す）— ユーザー承認済みの範囲

- recruit: `PLACEHOLDER` バッジ6件をHTMLコメントに置換（`準備中 — 近日公開予定` は残した）
- recruit: 「学びのまとめ」カード3枚をコメントアウト（`[要記入]` が大見出しとして見えていた）
- recruit: 「チームの雰囲気」セクションを `hidden`。ナビ・目次・フッターの3か所も併せて隠し、
  番号の穴を埋めるため「現場からの発信」を 07 → 06 に繰り上げた
- recruit: 他己紹介「氏名（仮）」カードを `hidden`
- index: 氏名が「氏　名」のカード2枚を `hidden`。代わりに看護師の補足文の冒頭に
  「ICUには専任の副師長2名と、集中ケア認定看護師が在籍しています。」を追加（承認済み）
- 両ページ: CKRT → **CKRT（CHDF）** に併記（実機ラベルが CHDF-3 のため。ユーザー選択）
- **index の設備の空枠4つは「隠さない」を選択したので残してある**

> ⚠️ **`[hidden]` が効かない罠。** `display: flex / grid` を指定した要素は、それだけで
> ブラウザ標準の `[hidden] { display: none }` を打ち消す。`style.css` の先頭に
> `[hidden] { display: none !important; }` を入れて解決した。**この1行に依存しているので消さないこと。**

> ⚠️ **「学びのまとめ」を `hidden` ではなくコメントアウトにした理由。** 親グリッドが
> `data-collapse-after="4"` で、`script.js` が4件目以降を `hidden` にしてトグルで開く。
> `hidden` のカードを残すと「もっと見る」で `[要記入]` のカードが開いてしまう。

### 第2段階（ヒーロー）— 実測値

背景に実際に描画された画素から計算。文字は純白。

| 箇所 | 変更前 | 変更後 |
|---|---|---|
| index ヒーロー最悪部 | **1.41:1** | **4.69:1** |
| index 数字カード内側 | 2.52:1 | **5.20:1** |
| recruit ヒーロー最悪部 | 2.27:1 | **5.17:1** |
| recruit 数字カード内側 | 2.67:1 | **5.85:1** |

やったこと（3つセット。1つだけでは足りない）:

1. `--hero-grad-soft` の不透明度 88〜93% → 96〜97%（`:root` とティール版の両方）
2. `#hero` の背景に `rgba(0,0,0,0.30)` の膜を1枚追加
3. ヒーローの文字の半透明をやめて `#fff` に統一

> ⚠️ **半透明の文字は使わない。** 背景の明るさが場所によって変わるため、
> 不透明度を下げると最も明るい隅で基準を割る（**実測 0.92 で 4.26:1 / 0.88 で 4.05:1**）。
> 強弱は文字サイズと太さで付ける。

> ⚠️ **数字カードの膜は白ではなく黒（`rgba(0,0,0,0.12)`）。** 白い膜を重ねると背景が
> 明るくなり、`白0.06` でもカード内側が 4.20:1（不合格）だった。黒に変えて 5.20:1。

### 第3段階（小さな不合格箇所・見出し階層）

| 内容 | 変更前 | 変更後 |
|---|---|---|
| 疾患タグ | 3.31:1 / 9px | **6.06:1 / 10px** |
| 診療理念の番号 01〜04 | 1.28:1 | **3.43:1**（28pxなので3:1でOK） |
| キャリアパスの小見出し | 3.06:1 / 10px | **7.86:1 / 11px** |
| 「裏側を見る」ボタン | 76×22px | **111×44px**（白地＋薄い枠） |
| スマホの申し込み欄の左右余白 | 6px | **16px** |

- 見出しの階層: `h2` の次に `h3` を飛ばして `h4`/`h5` が来ていた箇所を `h3` に統一
  （index 11か所 / recruit 18か所）。**CSSのセレクタも対で書き換えたので表示は変わっていない。**
  `.edu-callout` `.edu-feature` の `h4` は `h3` の下にあり正しい階層なのでそのまま
- index: 設備の左右交互が崩れていた。7項目なので **L R L R L R L**（以前は L R L R L L R）
- recruit: 「裏側を見る」4つのうち1つ目（8:30 回診）を最初から開いた状態に
- recruit: ヘッダーのコメントの「CTAは3か所」を2か所に修正

### 第4段階（文字サイズ・英語併記・目次）

文字サイズは**1行35〜45字に収まっていない箇所だけ**変えた。収まっている箇所は触っていない。
タイポスケールの変数（`--text-sm` など）も触っていない。

| 箇所 | 変更前 | 変更後 |
|---|---|---|
| index 看護師の補足文 | 14px / **1行67字** | 15px + `max-width:700px` → 約46字 |
| index 診療内容の導入文 | 15px / 48字 | 16px + `max-width:680px` → 約42字 |
| index 疾患カードの説明 | 12px | 14px / 行間1.8 |
| recruit 他己紹介の日本語 | 12px | 14px |
| recruit 教育体制の特長 | 12px / 行間1.6 | 13px / 行間1.8 |
| recruit キャリアパス本文 | 12px | 13px |
| recruit 他己紹介の英語 | 9px | 11px |

- **英語併記は全件、斜体をやめた。** 11〜12pxの斜体が日本語のすぐ下に来ると
  「読む対象ではなく視界に入る雑音」になっていた。消さずに通常字形にしてサイズを1段上げた。
  色は変えていない（薄くするとコントラスト基準を割る）
- `.section-subtitle` も斜体をやめた（英語が入ることも日本語が入ることもある行）
- ヒーローの改行: スマホで日本語と英語が地続きになっていた。改行を全部消す指定を
  `.hero-title` だけに限定し、英文は `.hero-desc span` を `block` にして必ず改行させる
- 目次（ジャンプナビ）:
  - `position: sticky` で固定（以前は `static` で、一度流れると二度と出なかった）
  - 右端に幅28pxのぼかし（「横にまだ続く」ことを示す。スクロールバーを隠しているため）
  - **480px以下は横スクロールをやめて3列2段。375pxで6項目すべてが最初から見える**
    （以前は4項目が画面外）。2段だと固定すると画面を占めすぎるのでこの幅では `static`
  - `scroll-padding-top` を 160px に（ヘッダー68px + 固定目次84px）。480px以下は80px

### 第5段階（ダミー枠・ヘッダー・残りのコントラスト）

- ダミー枠の「`ICU Beds · photo`」のような英語の補助表記を非表示に。monospace 指定と
  合わせて制作メモがそのまま公開されているように見えていた。日本語ラベルは通常字形13px。
  **HTMLには残してあるので、戻したいときは `.img-placeholder span.sub` の `display: none` を消す**
- **ヘッダーの「レジデント・フェロー募集 →」が 1280px で完全に画面外に出ていて押せなかった**
  （`#main-nav` は `overflow-x: auto` でスクロールバーを隠しているため手がかりもなし。
  1280pxで**201pxあふれ**）。幅を詰めて収める方法は文言の長さ次第で再発するので、
  このリンクを `sticky` で右端に貼り付けて、あふれても必ず残るようにした
- ヘッダーCTAの高さ 32px → **44px**
- スマホのメニューが半透明（0.98）で、下のヒーローの白文字が透けて読みにくかった → 不透明に
- セクション番号ラベル（`01 MEDICAL SERVICES` など）が 2.91:1 → **6.20〜6.27:1**
- `fonts.gstatic.com` への `preconnect` を追加

### 第6段階（病院外観の写真を活かす）— ユーザー指示で実施

**ユーザーからの指摘**: 「見やすくはなったが、病院の外観写真が活きなくなった」。

写真と文字を同じ場所に重ねている限り、どちらかが必ず犠牲になる:

- 透かして使う → 不透明度88〜97%の下では**写真は7〜12%しか見えず**、写真として成立しない
  （これは改修前からの状態。第2段階で3%になったので「消えた」と感じられた）
- 写真を見せる → 白文字が **1.41:1**（ほぼ読めない）

**採った解決**: 重ねるのをやめ、文字が乗らない場所で無加工のまま見せる。

3案を実際に作って比較した:

| 案 | 内容 | 結果 |
|---|---|---|
| **A（採用）** | ヒーローは無地、直下に全幅の写真帯 | 写真100%。コントラストは1つも犠牲にならない |
| B | ヒーローを左右に分け右46%を写真に | 写真は大きいが、**タクシーのナンバーが読める**ためぼかしが必要。スマホでは上下に積むので結果はAとほぼ同じ |
| C | いまの構造のまま膜に向きを付ける | **文字のある範囲が 2.22:1**。建物も緑にかぶって不自然。不採用 |

実装したもの:

- `.hero-photo-band`: ヒーロー直下の全幅の写真帯（PC 260px / スマホ 180px）。無加工。
  切り出しは `object-position: center 24%` で病院名の看板と青空が入る
- キャプション「沖縄県立中部病院（うるま市）」。本文と同じ列に左揃え
- `#hero` から写真と `--hero-grad-soft`（写真を透かすための半透明版）を削除

> ⚠️ **ヒーローの黒の膜は消さないこと。** 写真がなくなっても必要。無地の
> グラデーションだけだと、最も明るい端で白文字が **index 2.41:1 / recruit 2.73:1**。
> 写真が下にあったときは写真側が暗く効いていたため 0.30 で足りていたが、
> 写真を帯に移したので **0.38** に濃くしてある。

> ⚠️ **装飾の白は数字カードのコントラストを下げる。** 右上の大きな円（白4%）が
> カードの載る右上を明るくして 0.2 ほど落としていたので**黒4%に変えた**（見た目の
> 効果は同じ）。点模様も白0.07 → 0.05。**装飾の白を増やすときは必ず測り直すこと。**

> ⚠️ **数字カードの膜は計算どおりに効かない。** `backdrop-filter` の影響で実際の
> 描画は計算よりかなり明るく、0.12 → 0.20 に濃くしても 4.09 → 4.41:1 しか動かない。
> カード内側のコントラストは `#hero` 側の膜で担保している。

**キャプションの膜のつくり方（ハマりどころ）**: 単純な `linear-gradient(to top, 0.6, 0)`
にすると、**文字の位置ではまだ膜が 0.29〜0.46 しかなく**、雲の白が透けて 2.26:1 だった。
文字が乗る下44pxを `rgba(0,0,0,0.62)` で**一定**にしてから上へ抜くこと。現在 6.30:1 / 6.40:1。

#### 第6段階の実測（白文字との比・装飾も含めた本番と同じ状態）

| 箇所 | 帯を入れる前 | 帯を入れた後 |
|---|---|---|
| index 素の背景 | 4.69:1 | **5.09:1** |
| index 数字カード内側 | 5.20:1 | **4.97:1** |
| recruit 素の背景 | 5.17:1 | **5.57:1** |
| recruit 数字カード内側 | 5.85:1 | **5.51:1** |
| キャプション（両ページ） | — | **6.30:1 / 6.40:1** |

#### あわせて直したヘッダーの重なり

第5段階で固定したクロスリンクが、recruit の 1280px で最後のナビ項目「現場からの発信」に
重なっていた。左端のぼかしを 12px → 28px に広げ、1400px以下で項目の間隔を詰め、
recruit のロゴの補足「レジデント・フェロー募集」を省いた（ヒーローに大きく出ている）。
これで**両ページともあふれ0・重なりなし**。

### 副産物: ページが短くなった

| | 変更前（スマホ） | 変更後（スマホ） |
|---|---|---|
| recruit.html | 14,855px（約18画面分） | **12,264px** |
| index.html | 17,796px | 17,631px |

---

## 🔴 次にやること

### 1. ユーザーの回答を待っている（催促はしない）

| 項目 | 状態 |
|---|---|
| **index の最初の1画面の数字カード** | 「どこのことかよくわからない」との回答。**注釈付きの画面写真を見せて説明済み。** 下記 A 参照 |
| **修練医の声の直後の1行導線** | 未確認。新しい文章を足すことになるので承認が必要。下記 B 参照 |
| **index 設備セクションの短縮** | 未確認。下記 C 参照 |
| **Google Fonts のウェイト数削減** | 未確認。見た目が変わる可能性があるので触っていない |
| **写真帯の切り出し** | いまは「看板＋青空」（`object-position: center 24%`）。「正面入口＋植栽」にするなら `center 55%` 付近だが、**タクシーのナンバープレートが写るので先にぼかしが必要**。スマホでは看板が小さくなるので、専用の切り出し画像を用意する手もある（未実施） |
| 喜名副師長の名（下の名前） | 姓のみ表示中。判明したら `index.html` の `.staff-name` と `alt` の両方を直す |
| 他己紹介②の氏名 | カードごと `hidden` にしてある。決まったら `hidden` を外して `.peer-name` を差し替え |
| 他己紹介の3枠にイラストを入れるか | 未回答（`media/src/README.md` に切り出し手順あり） |
| 編集手順書（EDITING.md）を作るか | 未回答 |

#### A. index の最初の1画面（提案・未承認）

タグ4つと数字カード4つのうち、**2つが同じ内容の言い換え**になっている。

| | タグ（左下） | 数字カード（右上） |
|---|---|---|
| 1 | 12 ICUベッド | **12** / ICUベッド数 ← 重複 |
| 2 | 24時間対応 | **24** / 専門医師常駐 ← 重複 |
| 3 | 多職種チーム医療 | 5+ / 多職種連携 |
| 4 | 内科系・外科系集中治療 | 50+ / 教育病院の実績 |

いっぽう**ご家族が最初に知りたい面会時間（13:00〜18:00）は上から3,062px、全体の28%地点**。
スマホで指6画面分スクロールしないと出てこない。

**案**: 重複する2枚を `13:00-18:00 / 面会時間`（※時間外も相談可）と `随時 / 病状説明` に差し替える。
12床・24時間という事実は設備セクションと診療内容カードに既に書かれており失われない。

#### B. 修練医の声の直後の1行導線（recruit）

見学申し込みの導線は現在2本（ヘッダー固定と最下部）。ヘッダー固定があるのは長いページとして
正しい設計、という評価。ただし**気持ちが最も高まる「修練医の声」の直後に導線がない**。
大きなボタンではなく1行の誘導で十分（例「見学は随時受け付けています →」）。
**新しい文章を足すので承認が必要。**

#### C. index 設備セクションの短縮（提案）

index は設備セクション3,085px ＋ スタッフセクション3,068px ＝ **全体の57%**。
`script.js` に既にある「もっと見る」の仕組み（`data-collapse-after`、recruit で使用中）を
流用すれば**新規実装なしで**短縮できる（`index.html` の `<div class="equipment-list">` に
`id` と `data-collapse-after="3"` を付けるだけ）。
中身は消えず、押せば全部出る。ただし一般向けページで既定の表示量を減らす判断になるので承認が必要。

### 2. 原稿が必要（ユーザーが書くもの）

> 実物と照合した結果、**キャリアパス9件と機材ギャラリー4件はユーザー本人が記入済み**だった。
> 読者に見える `[要記入]` は現在0件。

| 場所 | 件数 | 中身 |
|---|---|---|
| recruit 学びのまとめ（コメントアウト中） | 9 | 3記事 ×（テーマ・一言コメント・参考文献） |
| recruit 修練医の声 3枠 | 3 | コメント本文（氏名 T.M・S.N・K.K は設定済み） |
| recruit 他己紹介 3枠 | 3 | 紹介文（うち1枠は氏名未定で `hidden`） |

差し替え予定の場所は `grep -n "差し替え予定" recruit.html` で出る。

進め方の合意事項: 文面を指定してもらってもよいし、「こういう内容を書きたい」という
箇条書きをもらえれば、こちらで文章に起こして提案する。

### 3. 公開前に必ずやること

| 項目 | 状態 |
|---|---|
| `noindex` / `robots.txt` | **外すのは病院としての正式許可が下りてから。** 技術的な手順ではなく許可に紐づくスイッチ（実名・住所を載せているため。`README.md` 参照）。逆に外し忘れると検索に一切載らない |
| 「修練医の声」「他己紹介」の本文 | **サンプル文のままバッジだけ外れている。公開前に要判断**（差し替え／カードを隠す／バッジを戻す）。他己紹介2件は実名＋実写真つき |
| `REFERRAL_ENABLED` | `script.js` 冒頭で `false`。紹介リンク発行機能。**正式許可が下りてから `true`** |
| 写真素材 | `img-placeholder` のダミー枠が recruit 12件 / index 10件（うち6件は意図的なスタッフアバター）。実写真の反映は正式許可後 |
| 隠している箇所を戻す | チームの雰囲気・学びのまとめ・氏名未記入のカード。戻し方は各HTMLのコメントに書いてある |

---

## 作業時の注意（実際に踏んだもの）

- **HTMLの一括置換は危険。** 行番号を指定して置換し、置換後に
  `staff-card` `staff-name` `placeholder-tag` などの**件数と `<div>` の開閉数を必ず数える**
- **`placeholder-tag` をクラス名で一括削除しない。** `準備中 — 近日公開予定` は残す対象
- **CSSを変えたら `index.html` と `recruit.html` の `?v=` を同じ値に上げる。**
  現在 `style.css?v=20260916f` / `script.js?v=20260915b`
- **ヘッドレスChromeの狭い幅（375px等）のキャプチャは信用しない。** レイアウトが
  もっと広い幅で描画され、実際には収まっているものがはみ出して見える。**このセッションでも
  1回引っかかった**（ヒーローの数字カードがはみ出して見えたが、実測は 16..181 / 195..359 で収まっていた）。
  レスポンシブの判定は `resize_window` + `javascript_tool` の実測を信じる。
  見た目をどうしても見たいときは Browser pane のスクリーンショット（スクロールしない状態で）
- **ヒーローのコントラストは文字ではなく背景を測る。** `.hero-inner { visibility: hidden }` を
  注入した使い捨てHTMLを作り、背景色だけを抽出してから計算する。カード内側は
  `.hero-text` と `.hero-stat` の中の文字だけを隠して別に測る（カードの矩形は
  `javascript_tool` で取る。範囲を取り違えると意味のない値が出る）
- **ユーザーが書いた日本語はユーザーの原稿。** 読みやすさに直結する場合に
  「こう分割すると読みやすい」と提案するに留め、内容を書き換えない
