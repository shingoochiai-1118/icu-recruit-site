# デザイン改修メモ（次セッション用）

作成: 2026-09-16 / 対象: `index.html`（トップ・緑） `recruit.html`（採用・ティール） `style.css`（共通）

プロのウェブデザイナー2名に、2ページを独立に評価してもらった結果をもとにした作業リスト。
**依頼主の狙いは「見やすさ最重視」「最後まで読まれること」の2点。**

> 作業のやり方（pull / push / ライブ確認 / `?v=` の扱い / 検証ツールの使い分け）は
> **`HANDOFF.md` の「1. この作業のやり方」を必ず先に読むこと。** ここでは繰り返さない。

---

## 0. 着手前に確認すること

### ① 未確認のまま残っている公開処理

`ef6c541`「師長の写真の代替テキストを 外間 美和子 に修正」を push 済みだが、
**GitHub Pages のビルドが15分以上 `building` のまま終わらず、ライブ反映を確認できていない**
（通常は20〜45秒）。GitHub側の遅延。まず次で確認する。

```bash
gh api repos/shingoochiai-1118/icu-recruit-site/pages/builds --jq '.[0] | "\(.status) \(.commit[0:7])"'
curl -s "https://shingoochiai-1118.github.io/icu-recruit-site/index.html?cb=$RANDOM" | grep -o 'alt="外間[^"]*"'
```

`alt="外間 美和子 師長"` が返れば解決済み。返らない場合、空コミットを1つ push して
ビルドを再度走らせる。

### ② ユーザーは GitHub の画面上で `recruit.html` を直接編集する

このセッション中も2回（`d7c29df` `e5000f0`）入った。**作業開始前に必ず `git pull`**。
作業が長くなる場合は途中でもう一度 pull し、コンフリクトしたら
**ユーザーが書いた文章を優先**する（rebase で解決した実績あり）。

### ③ ユーザーの承認をまだ得ていない

このリストは**提案段階**。第1段階の「隠す」対象をどこまでにするかを尋ねた状態で
セッションが終わっている。**着手前に承認を取ること。**

---

## 1. 評価の結論（要点だけ）

2名が独立に評価して、**1位と2位が完全に一致した**。

| 順位 | 内容 | 労力 | 効く範囲 |
|---|---|---|---|
| 1 | 「あとで書きます」というメモが読者に見えている | 小 | 両ページ |
| 2 | ヒーローの白文字が読めない（背景を暗くしないと直らない） | 小 | 両ページ（CSS共通） |
| 3 | 本文が小さい・目次がスマホで見切れる | 中 | 両ページ |

**良いと評価された点（壊さないこと）**

- 本文の色 5.15〜5.47:1 で基準クリア。「多くの病院サイトが薄いグレー文字で失敗するところ」
- 設備の説明文が専門語を噛み砕いてある
- スマホの横はみ出しゼロ（両ページ実測）
- `:focus-visible` のキーボード枠線が全体に実装済み。「ここまでやっているサイトは多くない」
- 画像の `alt` の使い分けが正しい（イラストは `alt=""`、実写は説明付き）
- **PCの1行あたりの文字数は、設備説明35字・ヒーロー42字などおおむね適正。スマホは20〜25字で良好。**
  → **行長は基本的に触らない。例外は下記 3-1 の1箇所だけ**

---

## 2. 第1段階：未完成に見えるものを隠す

**労力：小（1時間以内）／効果：最大。文章を書き足す必要はない。**

デザイナー2名がそろって1位に挙げた。要点は「書き足す」のではなく**「消す・隠す」**。
中身は残るので、写真や原稿が揃った時点で戻せる。

### 2-1. recruit.html の `PLACEHOLDER` タグ 6か所を消す

| 行 | 場所 |
|---|---|
| 549 / 563 / 577 | 修練医の声 3枚（`<span class="placeholder-tag">`） |
| 606 / 621 / 637 | 他己紹介 3枚（`<div class="placeholder-tag">`） |

**タグの行だけを消す。コメント本文は消さない**（内容は良く、タグを外せばそのまま読み物として成立する）。
差し替え予定の管理は HTML コメント（読者に見えないメモ）に移す。

> ⚠️ **最優先の理由**: 他己紹介の 606 と 621 は、**酒井先生・小波本さんの実名と実写真のすぐ上**にある。
> 読者に「この人の言葉ではないのか」と受け取られかねず、ご本人にとっても不本意な見え方になる。

> ⚠️ **`recruit.html:741` の `準備中 — 近日公開予定` は同じ `placeholder-tag` クラスだが残す。**
> これは読者向けの日本語として自然、という評価。クラス名で一括置換しないこと。

### 2-2. recruit.html の「学びのまとめ」カード3枚を非表示

`[要記入]` が**読者に見える形で9か所**ある（3枚 × 3か所）。

| カード | 見出し(h4) | 本文 | 参考文献 |
|---|---|---|---|
| 1枚目 | 770 | 771 | 774 |
| 2枚目 | 788 | 789 | 792 |
| 3枚目 | 806 | 807 | 810 |

見出しの `[要記入] テーマ（例：循環管理の文献レビュー）` が**そのまま大見出しとして表示**されている。
読み上げソフトの見出し一覧にも3つ並ぶ。**カードごと `hidden` にすれば同時に解決。**

「現場からの発信」セクションは、動画1本＋「準備中」カード1枚だけでも成立する。

> `[要記入]` は `recruit.html:416` `:467` にもあるが、**こちらは HTML コメント内なので読者には見えない**。触らなくてよい。

### 2-3. recruit.html「チームの雰囲気」セクションを非表示

`recruit.html:658` の `<section id="team">`。**写真4枚すべてが空枠**。
スマホでは縦に並ぶため、**斜線模様の空箱だけが画面3枚分続く**。

セクションを隠す場合、**目次とヘッダーメニューの「06 チームの雰囲気」も併せて隠す**こと
（`recruit.html:33` のナビ、`:99` のジャンプナビ）。リンク先が消えると目次が壊れる。

### 2-4. recruit.html 他己紹介の「氏名（仮）」カード

`recruit.html:629`。氏名が決まるまでカードごと非表示が無難。

### 2-5. index.html「氏　名」のスタッフカード2枚

`index.html:650`（副師長2人目）と `:657`（認定看護師）。
**県立病院の公式ページに未記入欄が出ている状態**で、ページ全体の信頼感を落としている、という指摘。

**推奨**: カード2枚を削除し、既存の看護師補足文（`index.html:664`）の冒頭に一文足す。

> 「ICUには専任の副師長2名と、集中ケア認定看護師が在籍しています。」

同意確認が取れ次第カードを戻せる。**文章はユーザーの原稿なので、この一文の追加は必ず承認を取る。**

> 関連: **喜名副師長の名（下の名前）が未確定**のまま。判明したら `index.html:642` 付近の
> `.staff-name` と `alt` の両方を直す。

### 2-6. index.html 設備セクションの大きな空枠4つ

| 行 | 項目 |
|---|---|
| 368 | PiCCOモニター |
| 416 | ECMO |
| 426 | Impella |
| 446 | ICU病床 |

PCで高さ300px、スマホで220pxの点線の空箱。7項目中4回、読者の視線が何もない場所へ向く。

**推奨**: 写真のない項目は画像側の要素を省き、説明文を横幅いっぱい（1カラム）にする。
枠を残す場合も、最低限 `· photo` という英語表記は外す（`style.css:160` が monospace 指定で、
制作メモにしか見えない）。

> ⚠️ `index.html` の `img-placeholder` は全10か所だが、うち6か所
> （`:567 :574 :581 :599 :649 :656`）は**80pxの丸いスタッフアバター**で、
> 「医」「看」「認」の文字が入った**意図的なデザイン**。**これは隠さない。**
> 隠す対象は上記の設備4つだけ。

### 2-7. recruit.html 機材ギャラリーの空枠3つ

4枚中、実写はCKRTのみ（PiCCO・スワンガンツ・脳波計が空）。
写真のあるCKRTだけ残すか、写真なしでも成立するテキスト一覧に切り替える。

---

## 3. 第2段階：ヒーローの白文字を読めるようにする

**労力：小／効果：大／CSSは共通なので1回の修正で両ページに効く。**

### 3-0. 実測値（これが出発点）

背景に実際に描画された画素から計算した値。基準は WCAG AA（本文4.5:1 / 大きい文字3:1）。

| ページ | 箇所 | 実測 | 判定 |
|---|---|---|---|
| index | 建物の白壁が透けている部分 | **1.41:1** | ほぼ見えない |
| index | 本文中の英語（`index.html:55`） | 1.99:1 | 不合格 |
| index | 数字カード右下 | 2.52:1 | 不合格 |
| recruit | `cases / year` | 2.27:1 | 不合格 |
| recruit | 数字ラベル | 2.67:1 | 不合格 |
| recruit | 説明文 | 3.26:1 | 不合格 |
| recruit | 大見出し54px | 4.04:1 | 合格（大きい文字として） |

**最重要の発見（2名が別々に同じ結論に達した）**

> **文字を真っ白（不透明度100%）にしても4.04:1にしかならない。
> 「文字を濃くする」だけでは本文サイズの基準を満たせない。背景側を暗くする必要がある。**

実害は「12床」「24時間」「700+」「10〜15」という**両ページで最も強い武器である数字の"説明ラベル"が読めない**こと。数字だけ見えても何の数字か分からなければ効果が半減する。

### 3-1. 変更内容（3つをセットで行う。1つだけでは足りない）

**(a) 背景写真を抑える** — `style.css:38`（`:root`）と、ティール版（`:root[data-theme="recruit"]`）の両方

```
現在(index): rgba(47,138,76,0.93) / rgba(70,160,99,0.90) / rgba(95,185,124,0.88)
変更案:      rgba(47,138,76,0.97) / rgba(70,160,99,0.97) / rgba(95,185,124,0.96)
```

建物はうっすら残るが、白壁の透け（1.41:1の原因）が消える。
**ティール側も同じ考え方で不透明度を上げること。**

**(b) 薄い黒の膜を1枚重ねる** — `style.css:307`

```css
background-image:
  linear-gradient(rgba(0,0,0,0.30), rgba(0,0,0,0.30)),
  var(--hero-grad-soft),
  url("media/photos/hospital-exterior.jpg");
```

**(c) 文字の半透明をやめる**

| 行 | セレクタ | 現在 | 変更案 |
|---|---|---|---|
| 332 | `.hero-badge span` | `rgba(255,255,255,0.9)` | `#fff` |
| 336 | `.hero-title-en` | `rgba(255,255,255,0.65)` | `rgba(255,255,255,0.92)` |
| 337 | `.hero-desc` | `rgba(255,255,255,0.82)` | `#fff` |
| 341 | `.hero-tag` | 文字 `0.92` / 背景 `0.14` | 文字 `#fff` / 背景 `0.22` |
| 358 | `.hero-stat .unit` | `rgba(255,255,255,0.6)` | `rgba(255,255,255,0.88)` |
| 359 | `.hero-stat .label` | `rgba(255,255,255,0.8)` | `#fff` |

加えて `index.html:55` のインライン指定 `rgba(255,255,255,0.55)` / 12px →
`rgba(255,255,255,0.88)` / 13px。`recruit.html:57` にも同種の指定あり。

**(d) 数字カードの白フィルムを薄くする**

カードは半透明の白を重ねているぶん背景がさらに明るい（recruit実測 `rgb(64,156,142)`）。
カードの背景の白の不透明度を下げると、それだけでも改善する。

### 3-2. 期待値

上記 (a)(b)(c) を行った場合、**最も条件の悪い箇所でも約4.8:1**（本文基準4.5:1をクリア）。
index担当の計算値（背景 `rgb(108,178,129)` に30%の黒を重ねた場合）。

### 3-3. 検証方法

**文字ではなく背景を測ること。** 過去に文字の画素を拾って 1.00:1 という無意味な値を出した。
`.hero-inner { visibility: hidden }` を注入した使い捨てHTMLを作ってスクリーンショットを撮り、
背景色だけを抽出してから計算する（このセッションで実績のある手順）。

---

## 4. 第3段階：文字サイズ・行長・目次

**労力：中。第1・第2段階のあとで良い。**

### 4-1. スマホでヒーローの日本語と英語が地続きになる（両ページ）

`style.css:990`（`@media (max-width: 768px)` 内）

```css
.hero-desc br, .hero-title br { display: none; }
```

スマホで改行が全部無効になり、実機（375px）ではこう見える:

> 全国屈指の教育病院で、集中治療の最前線に立つ。 豊富な症例と手厚い指導体制の中で、確かな臨床力を鍛えます。 *Train at the frontline of critical care...*

**変更案**: 英語の `<span>` を `display:block; margin-top:10px;` にして必ず改行させ、
`style.css:990` は `.hero-title br` だけに限定する（日本語2文の改行は活かす）。

### 4-2. 本文の文字サイズ

**⚠️ `style.css:54` 付近のタイポスケール変数（`--text-sm-plus` など）を変えると両ページの全カードに同時に効く。**
影響が読みにくいので、**個別クラスで調整するほうが安全**。

実測（PC 1280px）:

| ページ | 箇所 | 現在 | 変更案 |
|---|---|---|---|
| index | 看護師の補足文 `index.html:664` | 14px / **1行67字** | 15px + `max-width:700px` → 約46字 |
| index | 診療内容の導入文 `index.html:111` | 15px / 48字 | 16px + `max-width:680px` → 約42字 |
| index | 疾患カードの説明 | 12px | 14px |
| recruit | 他己紹介の日本語 `.peer-comment` | 12px | 14px |
| recruit | 教育体制の特長 `.edu-feature p` | 12px / **行間1.6** | 13px / 行間1.8 |
| recruit | キャリアパス本文 `.career-stage-body dd` | 12px | 13px |
| recruit | 他己紹介の英語 `style.css:733` | **9px** | 11px |

> **1行67字の1箇所（`index.html:664`）だけでも効果が大きい。** 迷ったらここから。
> 逆に、35〜45字に収まっている箇所は触らない。

### 4-3. 英語併記の扱い（両ページ）

11〜12pxの斜体で、日本語の可読性を下げている（「読む対象ではなく視界に入る雑音」）。
**消す必要はなく、視覚的に分けるだけで解決する。**

- 斜体（`font-style: italic`）をやめて通常字形に、色をもう一段薄く
- サイズは 11px → 12px（斜体をやめれば12pxでも日本語の邪魔にならない）

### 4-4. 目次（ジャンプナビ）— recruit が深刻

**recruit**: 目次の中身が731px、スマホ画面375px。
**「05 他己紹介・06 チームの雰囲気・07 現場からの発信」は横スクロールしないと存在に気づけない。**
しかも `#jump-nav`（`style.css:370`）は `position: static` で画面に固定されず、一度流れると二度と出ない。
ページ丈はスマホで14,855px（**約18画面分**）。現在地が分からないのが離脱の最大要因。

**index**: 6項目中5項目までしか見えず、「06 スタッフ紹介」が画面外。

**変更案**

```css
/* 共通: 画面上部に貼り付ける */
#jump-nav { position: sticky; top: <ヘッダーの高さ>; z-index: 50; }

/* 横に続くことを示す: .jump-nav-inner に ::after で幅24pxの白→透明グラデーション */

/* index向け: 480px以下で3列2段にして6項目すべて見せる */
@media (max-width: 480px) {
  .jump-nav-inner { display: grid; grid-template-columns: repeat(3, 1fr); overflow-x: visible; }
  .jump-item { padding: 12px 6px; }
}
```

recruitは7項目なので、スマホで英語表記を隠し `.jump-item` の左右padding 16px→10px に詰めると
5〜6項目が最初から見える。

> `.jump-nav-inner` は `scrollbar-width: none`（`style.css:378`）でスクロールバーを隠しているため、
> 横に続く手がかりが本当に何もない状態。ぼかしを足すのが最低限。

---

## 5. すぐ直せる小さなもの（まとめて1コミットで良い）

| # | 内容 | 場所 | 変更 | 影響 |
|---|---|---|---|---|
| 1 | 設備の左右交互レイアウトが1か所崩れている | `index.html:444` | `left` → `right` | index |
| 2 | 申し込み欄だけスマホで文字が画面の端から6px（他は16px） | `style.css:987` | `padding: 3.5rem 0` → `3.5rem 1rem` | **両ページ** |
| 3 | 共有リンクで開くと見出しがヘッダーに隠れる | `style.css:2` | `html` に `scroll-padding-top: 80px;` | 両ページ |
| 4 | 疾患タグが 3.31:1・9px | `style.css:445` | `color: var(--primary)` → `var(--primary-dark)` で **6.06:1**、9px→10px | index |
| 5 | 診療理念の番号01〜04が **1.28:1**（ほぼ見えない） | `style.css:427` | `var(--primary-light)` → `var(--primary)` で 3.43:1（28pxなので基準3:1クリア） | index |
| 6 | キャリアパスの小見出しが 3.06:1・10px | `style.css:630-631` | `var(--accent)` → `var(--primary-dark)` で **7.86:1**、10px→11px | recruit |
| 7 | 「裏側を見る」ボタンが 76×22px（推奨44px） | `style.css:515` | `padding: 3px 0` → `11px 0`、文字11px→12px、薄い枠か背景を付けて押せると分かるように | recruit |

> **#4 について**: 設備セクションのタグは既に `--primary-dark`（5.98:1）を使っている。
> 疾患タグをそちらに合わせる＝サイト内の統一でもある。

### 5-1. 見出しの階層が飛んでいる（読み上げソフト利用者に影響）

`h2` の次に `h3` を飛ばして `h4` が来る箇所。**見た目は一切変えずに直せる。**

- index（11か所）: `:175 :182 :189 :196`（診療理念）、`:320 :324 :328`（1日の流れ）、
  `:675`（募集）、`:708 :713 :723`（フッター）
- recruit（4か所）: `:147 :193 :239 :268`（「裏側を見る」の中の h5 → h4 へ）

HTMLの `<h4>` を `<h3>` にし、**あわせて CSS 側のセレクタも書き換える**
（`style.css:503 :931 :946` および `.value-item h4` など）。これで表示は同じまま。

### 5-2. 「裏側を見る」の最初の1つを開いた状態にする

**労力の小ささに対して効果が大きい**と recruit 担当が強く推していた項目。

中身（「回診が始まる前の60分」など）は**このページで最も具体的で、最も研修医が知りたい情報**なのに、
小さな補助リンクに見えるため多くの読者が開かずに通り過ぎている。
**1つ目（8:30 回診）を最初から開いておくと「ここは開くものだ」と学習され、残り3つも開かれる。**

> 動作自体は実際に押して確認済みで、開閉・アイコンの `+`/`−` 切り替え・ラベル変更・
> `aria-expanded` の更新すべて正常。**JSは触らなくてよい。**

### 5-3. 見学申し込みの導線（recruit）

現在**2本**（ヘッダー固定 `:39` と最下部 `:830`）。
`recruit.html:38` の HTML コメントは「3本」と書いてあるが、9/15に2本を1本へ統合した際の
**書き換え漏れ。ついでに直すこと。**

ヘッダー固定CTAがあるのは長いページとして正しい設計、という評価。ただし

- **気持ちが最も高まる「修練医の声」の直後（`recruit.html:592` 付近）に導線がない。**
  大きなボタンでなく1行の誘導で十分（例「見学は随時受け付けています →」）
- ヘッダーCTAは実測 113×32px で、推奨タップ領域44pxに不足

---

## 6. 第1段階の前に、ユーザーの判断が要るもの

| 項目 | 内容 |
|---|---|
| **隠す範囲** | 「チームの雰囲気」セクション／「学びのまとめ」3枚／index設備の空枠4つを、公開ページから一時的に消してよいか。**中身は消えず、写真や原稿が揃えば戻せる**ことを併せて伝える |
| **index の最初の1画面** | 下記 6-1 の差し替え案。数字カードの内容に関わるので独断で変えない |
| **喜名副師長の名** | 未確定のまま |
| **CKRT / CHDF-3** | 実機ラベルは `CHDF-3`、サイト表記は CKRT。どちらに揃えるか |
| **看護師補足文への一文追加** | 2-5 の「ICUには専任の副師長2名と…」。**ユーザーの原稿に手を入れるので必ず承認を取る** |

### 6-1. index の最初の1画面（提案のみ・未承認）

タグ4つ（`index.html:57-62`）と数字カード4つ（`:64-85`）を並べると、**4つのうち3つが同じ内容の言い換え**。

| | タグ | 数字カード |
|---|---|---|
| 1 | 12 ICUベッド | **12** ICUベッド数 |
| 2 | 24時間対応 | **24** 専門医師常駐 |
| 3 | 多職種チーム医療 | **5+** 多職種連携 |
| 4 | 内科系・外科系集中治療 | **50+** 教育病院の実績 |

いっぽう**ご家族が最初に知りたい面会時間（13:00〜18:00）は上から3,062px、全体の28%地点**。
スマホで指6画面分スクロールしないと出てこない。

**案**: 重複する「12 beds」「24 hrs」のカードを
`13:00-18:00 / 面会時間`（※時間外も相談可）と `随時 / 病状説明` に差し替える。
12床・24時間という事実は設備セクション（`:455`）と診療内容カード（`:120`）に既に書かれており失われない。

---

## 7. 作業時の注意（このセッションで実際に踏んだもの）

- **HTMLの一括置換は危険。** 過去2回 `index.html` を壊している。置換後は
  `staff-card` `staff-name` `placeholder-tag` などの**件数が想定どおりか必ず数える**
- **`placeholder-tag` をクラス名で一括削除しない。** `recruit.html:741` の
  「準備中 — 近日公開予定」は残す対象
- **CSSを変えたら `index.html` と `recruit.html` の `?v=` を同じ値に上げる。**
  現在 `style.css?v=20260915i` / `script.js?v=20260915b`（両ページ一致済み）
- **写真の差し替えが発生したら `media/src/prepare-photo.sh` を通す**（Exif/GPS削除）。
  原寸で患者情報・職員氏名の写り込みを確認すること
- **ヘッドレスChromeで `offsetTop` を使う場合、Browser pane を先に1280px幅にする**
  （幅が違うと切り出し位置がずれる。HANDOFF §1④）
- **ユーザーが書いた日本語はユーザーの原稿。** 読みやすさに直結する場合（段落が長すぎる等）に
  「こう分割すると読みやすい」と提案するに留め、内容を書き換えない

---

## 8. 参考：ページの規模（実測）

| | PC | スマホ |
|---|---|---|
| index.html | 10,815px | 17,796px（390px幅・約21画面分） |
| recruit.html | — | 14,855px（375px幅・約18画面分） |

index は設備セクション3,085px ＋ スタッフセクション3,068px ＝ **全体の57%**。
`script.js` に既にある「もっと見る」の仕組み（`data-collapse-after`、recruitで使用中）を
設備セクションに流用すれば、**新規実装なしで**短縮できる（`index.html:345` の
`<div class="equipment-list">` に `id` と `data-collapse-after="3"` を付ける）。
